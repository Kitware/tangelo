$(function () {
    // A model describing a file that contains a Lyra-edited visualization.
    LyraVis = Backbone.Model.extend({
    });

    // A collection describing all the Lyra vis files in a Girder instance.
    LyraVisList = Backbone.Collection.extend({
    });

    // A view that renders a LyraVis model.
    VegaView = Backbone.View.extend({
    });

    // A model describing a file that contains data for a Vega visualization.
    DataFile = Backbone.Model.extend({
    });

    // A collection describing all the data files in a Girder instance.
    DataFileList = Backbone.Collection.extend({
    });

    // A view that renders a list of files.
    FileListView = Backbone.View.extend({
    });
});
